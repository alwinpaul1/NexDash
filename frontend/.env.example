# TomTom Map Display API key for the Fleet Map Overview real-map tiles.
# Get a free key at https://developer.tomtom.com (Maps > Map Display API).
# Copy this file to `.env` and paste your key. Without it, the map falls back
# to the free CartoDB basemap.
VITE_TOMTOM_API_KEY=
